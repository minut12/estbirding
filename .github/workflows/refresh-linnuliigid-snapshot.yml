name: Refresh linnuliigid snapshot

on:
  schedule:
    - cron: '0 */6 * * *'   # Every 6 hours
  workflow_dispatch:          # Manual trigger

jobs:
  refresh:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - name: Trigger snapshot refresh and wait for completion
        env:
          FUNCTION_URL: https://eenwcyuyugyrjgpivxrq.supabase.co/functions/v1/linnuliigid-snapshot
          SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
        run: |
          set -uo pipefail   # NOTE: no -e; explicit per-call error handling

          MAX_POLLS=200
          SLEEP_BETWEEN=15
          STUCK_THRESHOLD_SEC=600   # if progress doesn't move for 10 min, give up

          # Tolerant extractors — never crash the script.
          extract_field() {
            local field="$1" file="$2"
            [ -s "$file" ] || { echo ""; return 0; }
            grep -o "\"$field\"[[:space:]]*:[[:space:]]*\"[^\"]*\"" "$file" 2>/dev/null \
              | head -1 \
              | sed -E "s/.*\"$field\"[[:space:]]*:[[:space:]]*\"([^\"]*)\".*/\1/" \
              || echo ""
          }
          extract_number() {
            local field="$1" file="$2"
            [ -s "$file" ] || { echo ""; return 0; }
            grep -o "\"$field\"[[:space:]]*:[[:space:]]*[0-9]\+" "$file" 2>/dev/null \
              | head -1 \
              | sed -E "s/.*\"$field\"[[:space:]]*:[[:space:]]*([0-9]+).*/\1/" \
              || echo ""
          }

          do_post() {
            local out="$1" body="$2" max_time="$3"
            : > "$out"
            local code
            code=$(curl -sS --max-time "$max_time" -o "$out" -w '%{http_code}' \
              -X POST "$FUNCTION_URL" \
              -H "Content-Type: application/json" \
              -H "Authorization: Bearer $SERVICE_ROLE_KEY" \
              -d "$body" 2>/dev/null) || code="000"
            [ -s "$out" ] || echo '{}' > "$out"
            echo "${code:-000}"
          }

          echo "=== Step 1: Read baseline state ==="
          BASE_CODE=$(do_post /tmp/base.json '{"action":"start_refresh"}' 30)
          echo "HTTP $BASE_CODE"
          head -c 400 /tmp/base.json; echo
          INITIAL_GEN=$(extract_field generated_at /tmp/base.json)
          echo "Step 1 generated_at: '$INITIAL_GEN'"

          echo "=== Step 2: Trigger refresh (fire-and-forget, long timeout) ==="
          TRIG_CODE=$(do_post /tmp/trig.json '{"action":"start_refresh","force":true}' 90)
          echo "HTTP $TRIG_CODE (timeout/error here is OK — refresh likely already running)"
          head -c 400 /tmp/trig.json; echo
          # Fall back to trigger response for baseline if Step 1 didn't have it
          if [ -z "$INITIAL_GEN" ]; then
            INITIAL_GEN=$(extract_field generated_at /tmp/trig.json)
            echo "Step 2 fallback generated_at: '$INITIAL_GEN'"
          fi
          echo "Final baseline generated_at: '${INITIAL_GEN:-<none>}'"
          if [ -z "$INITIAL_GEN" ]; then
            echo "WARNING: no baseline generated_at — will accept any ready response as success."
          fi

          echo "=== Step 3: Poll until snapshot ready ==="
          LAST_PROGRESS=-1
          LAST_PROGRESS_AT=$(date +%s)

          for i in $(seq 1 $MAX_POLLS); do
            echo "--- Poll $i / $MAX_POLLS ---"
            CODE=$(do_post /tmp/resp.json '{"action":"start_refresh"}' 30)
            echo "HTTP $CODE"
            head -c 300 /tmp/resp.json; echo

            STATUS=$(extract_field status /tmp/resp.json)
            GEN=$(extract_field generated_at /tmp/resp.json)
            DONE=$(extract_number progress_done /tmp/resp.json)
            TOTAL=$(extract_number progress_total /tmp/resp.json)
            echo "parsed: status='$STATUS' gen='$GEN' progress=${DONE:-?}/${TOTAL:-?}"

            # Success conditions:
            # (a) status=ready AND generated_at advanced past baseline, OR
            # (b) status=ready AND we never had a baseline (best-effort)
            if [ "$STATUS" = "ready" ]; then
              if [ -n "$INITIAL_GEN" ] && [ -n "$GEN" ] && [ "$GEN" != "$INITIAL_GEN" ]; then
                echo ""
                echo "✓ Snapshot refresh completed successfully (fresh)."
                echo "  baseline generated_at: $INITIAL_GEN"
                echo "  final    generated_at: $GEN"
                exit 0
              fi
              if [ -z "$INITIAL_GEN" ]; then
                echo ""
                echo "✓ Snapshot is ready (no baseline available, accepting)."
                echo "  generated_at: $GEN"
                exit 0
              fi
              echo "  (status ready but generated_at unchanged — same snapshot, keep waiting)"
            fi

            # Stuck detection: progress hasn't advanced in STUCK_THRESHOLD_SEC
            NOW=$(date +%s)
            if [ -n "$DONE" ] && [ "$DONE" != "$LAST_PROGRESS" ]; then
              LAST_PROGRESS="$DONE"
              LAST_PROGRESS_AT=$NOW
            fi
            ELAPSED=$((NOW - LAST_PROGRESS_AT))
            if [ "$ELAPSED" -gt "$STUCK_THRESHOLD_SEC" ]; then
              echo ""
              echo "✗ ERROR: progress stuck at $LAST_PROGRESS for ${ELAPSED}s (>${STUCK_THRESHOLD_SEC}s threshold). Aborting."
              exit 1
            fi

            if [ "$CODE" = "429" ]; then
              echo "Cooldown — waiting 60s..."
              sleep 60
              continue
            fi

            sleep $SLEEP_BETWEEN
          done

          echo ""
          echo "✗ ERROR: Did not see a fresh ready snapshot after $MAX_POLLS polls (~$((MAX_POLLS * SLEEP_BETWEEN / 60)) min)."
          echo "  baseline generated_at: ${INITIAL_GEN:-<none>}"
          echo "  last seen: status='$STATUS' gen='$GEN' progress=${DONE:-?}/${TOTAL:-?}"
          exit 1
