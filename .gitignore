# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

.env
.env.*
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Dev tool / scratch directories
.lovable/
.scratch/
.tmp_diag/
.claude/
.playwright-mcp/

# Supabase CLI local scratch state (link target, version pins) — not source.
supabase/.temp/

# Graphify knowledge-graph outputs — rebuilt locally via the post-commit hook;
# keep out of the Lovable-synced repo.
graphify-out/

# Local MCP server config — contains a loopback-only bearer token; never commit.
.mcp.json

public/maps/**/*.bak

# Ennustus scratch (P0–P2 outputs, GBIF facet cache, Claude Code scratch)
tmp/gbif-cache/
tmp/backtest_*.csv
tmp/backtest_*.md
tmp/phenology_draft*.csv
tmp/phenology_draft_summary.md
tmp/phenology_review_batch*.md
Claude outputs/
