# Events schema fix

Run `docs/supabase/events_schema_fix.sql` in Supabase SQL Editor once for your project.

1. Open Supabase Dashboard -> SQL Editor.
2. Paste the SQL from `docs/supabase/events_schema_fix.sql`.
3. Run it.
4. Re-run safely anytime; statements are idempotent.
