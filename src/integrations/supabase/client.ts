// This file is automatically generated. Do not edit it directly.
import { createClient, type SupabaseClient } from "@supabase/supabase-js";
import type { Database } from "./types";
import { getSupabaseAnonKey, getSupabaseUrl, validateSupabaseConfig } from "@/config/supabaseConfig";

let supabaseInitError: string | null = null;
let supabase: SupabaseClient<Database> | null = null;

const validation = validateSupabaseConfig();
if (!validation.ok) {
  supabaseInitError = validation.error || "Supabase config invalid";
  console.warn(`[supabase] ${supabaseInitError}`);
} else {
  try {
    supabase = createClient<Database>(getSupabaseUrl(), getSupabaseAnonKey(), {
      auth: {
        storage: localStorage,
        persistSession: true,
        autoRefreshToken: true,
      },
    });
  } catch (error) {
    supabase = null;
    supabaseInitError = error instanceof Error ? error.message : String(error);
    console.warn(`[supabase] ${supabaseInitError}`);
  }
}

export { supabase, supabaseInitError };
